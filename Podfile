# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'FoursquareAPI Nearest Places' do
use_frameworks!
      pod 'Alamofire', '~> 3.5'
      pod 'SwiftyJSON', '~> 2.3'
end
